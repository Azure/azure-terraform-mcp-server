#################################################################################
#                               OneBranch Pipelines                             #
# Documentation:  https://aka.ms/obpipelines                                    #
# Yaml Schema:    https://aka.ms/obpipelines/yaml/schema                        #
# Retail Tasks:   https://aka.ms/obpipelines/tasks                              #
#################################################################################

# CI trigger: batch pushes to main
trigger:
    batch: true
    branches:
        include:
            - main

resources:
    repositories:
        - repository: templates
          type: git
          name: OneBranch.Pipelines/GovernedTemplates
          ref: refs/heads/main

extends:
    template: v2/OneBranch.Official.CrossPlat.yml@templates
    parameters:
        globalSdl:
            tsa:
                enabled: false
        stages:
            #------------------------------------------------------------
            # Stage: Linux Unit Tests
            #------------------------------------------------------------
            - stage: LinuxUnitTests
              dependsOn: []
              jobs:
                  - job: UnitTests
                    pool:
                        type: linux
                    variables:
                        ob_outputDirectory: '$(Build.SourcesDirectory)/out'
                        ob_artifactBaseName: 'drop_UnitTests_$(NODE_VERSION)'
                        LinuxContainerImage: 'mcr.microsoft.com/onebranch/azurelinux/build:3.0'
                    strategy:
                        matrix:
                            Node20:
                                NODE_VERSION: '20.x'
                            Node22:
                                NODE_VERSION: '22.x'
                    steps:
                        - task: NodeTool@0
                          inputs:
                              versionSpec: $(NODE_VERSION)
                          displayName: 'Install Node.js $(NODE_VERSION)'
                        - task: npmAuthenticate@0
                          inputs:
                              workingFile: .npmrc
                          displayName: 'Authenticate npm feed'
                        - script: npm ci
                          displayName: 'npm ci'
                        - script: npm run build
                          displayName: 'npm run build'
                        - script: npm run lint
                          displayName: 'npm run lint'
                        - script: npm run typecheck
                          displayName: 'npm run typecheck'
                        - script: npm test
                          displayName: 'Run unit tests'

            #------------------------------------------------------------
            # Stage: Build & Package
            # Produces .tgz artifact via ob_outputDirectory
            #------------------------------------------------------------
            - stage: Build
              dependsOn:
                  - LinuxUnitTests
              jobs:
                  - job: Build
                    pool:
                        type: linux
                    variables:
                        ob_outputDirectory: '$(Build.ArtifactStagingDirectory)/out'
                        ob_git_checkout: true
                        LinuxContainerImage: 'mcr.microsoft.com/onebranch/azurelinux/build:3.0'
                    steps:
                        - task: NodeTool@0
                          inputs:
                              versionSpec: '20.x'
                          displayName: 'Install Node.js 20.x'
                        - task: npmAuthenticate@0
                          inputs:
                              workingFile: .npmrc
                          displayName: 'Authenticate npm feed'
                        - script: npm ci
                          displayName: 'npm ci'
                        - script: npm audit --production
                          displayName: 'Run vulnerability scan'
                        - script: npm run build
                          displayName: 'npm run build'
                        - task: CopyFiles@2
                          displayName: 'Copy files to staging'
                          inputs:
                              sourceFolder: '$(Build.SourcesDirectory)'
                              contents: |
                                  dist/**
                                  LICENSE
                                  package.json
                                  README.md
                              targetFolder: '$(Build.ArtifactStagingDirectory)/dropInput'
                              cleanTargetFolder: true
                        - script: npm prune --production
                          displayName: 'npm prune --production'
                        - script: npm pack
                          displayName: 'npm pack'
                          workingDirectory: '$(Build.ArtifactStagingDirectory)/dropInput'
                        # Move .tgz to ob_outputDirectory for automatic artifact publishing
                        - script: mkdir -p $(ob_outputDirectory) && mv $(Build.ArtifactStagingDirectory)/dropInput/*.tgz $(ob_outputDirectory)/
                          displayName: 'Move package to output directory'
